# Copy to .streamlit/secrets.toml for local Streamlit runs (do not commit real keys).
# On Streamlit Cloud, paste this into App settings → Secrets.

GROQ_API_KEY = "gsk_your_key_here"
LLM_PROVIDER = "groq"
LLM_MODEL = "llama-3.1-8b-instant"
